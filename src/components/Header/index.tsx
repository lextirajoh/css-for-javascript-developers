import { HeaderContainer, HeaderContainerTitle } from './styled';

export const Header = () => {
  return (
    <>
      <HeaderContainer>
        <HeaderContainerTitle>CSS FOR JS DEVELOPERS</HeaderContainerTitle>
      </HeaderContainer>
    </>
  );
};
